version: "3.8"
services:
  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - 8888:80
    volumes:
      - /opt/docker/dashboard:/usr/share/nginx/html:ro
      - /opt/docker/dashboard/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
